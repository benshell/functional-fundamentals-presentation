## Lambda Calculus Identity Function

λx.x

Or in JS:

```js
const identity = (x) => x
```

Any modern language that uses Lambda concepts is going
to have built-in data types. But it is important to understand
that in Lambda Calculus literally EVERYTHING is defined in terms
of functions.
